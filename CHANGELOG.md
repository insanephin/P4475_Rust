# 변경 기록

이 문서는 사용자에게 영향을 주는 주요 변경점을 한국어로 정리합니다.

## 1.0.0 — 2026-08-14

P4475 Rust의 첫 정식 버전입니다. 0.3.0의 외부 자산 가져오기 기반 위에
DataRaw 호환성 검사와 시험용 XUN 주행 사이드카를 통합했습니다.

### XUN 카트 시험 지원

- 최신 클라이언트의 `defaultExceedType` 1~4를 사용하는 XUN 카트 100종을
  자산 가져오기 후보에 추가했습니다. 서버는 특정 카트 ID를 하드코딩하지
  않고 가져온 BodyParam/KartSpec을 읽어 카트별 프로필을 선택합니다.
- S/B/L 스피드형 카트의 부스터 사용 횟수, 부능효과 충전·발동·종료,
  여섯 가지 물리 소비식, 충돌/드리프트 게이지 보정과 경기 시작 시 상태
  초기화를 연결했습니다.
- 부능효과 게이지를 연속형 UI로 표시하고, 부능효과 aura와 순정 Exceed
  효과를 서로 독립된 상태와 렌더링 경로로 처리합니다.
- 최신 카트의 차체·기본 파츠 수치를 4475 정보창 표시값으로 변환합니다.
  흑기사 XUN은 `드리프트 1158 / 가속력 1159 / 코너링 1050 /
  부스터 시간 1054`로 표시됩니다.
- `defaultExceedType=1`인 아이템형 카트는 서버의 개인전 아이템 확률표를
  이용해 경기 시작 아이템을 지급하고 기존 카트 아이템 변환 규칙을
  적용합니다.
- 서버와 DLL 사이에 전용 TCP 보조 채널(기본 `39314`)을 추가했습니다.
  게임 프로토콜과 분리된 이 채널로 현재 카트 프로필과 경기 세대 정보를
  전달합니다.
- 32비트 MSVC 훅/DLL 프로젝트 전체 소스, CMake 빌드 정의와 네 가지
  스모크 테스트를 `native/p4475-xun-sidecar`에 공개했습니다. 클라이언트
  원본 파일, RHO 내용, IDB 및 디컴파일 산출물은 포함하지 않습니다.

### 접속기와 DataRaw 안전성

- 서버 시작과 클라이언트 실행에 각각 `DataRaw 사용` 옵션을 두었습니다.
  양쪽이 사용하기로 한 경우 로그인 전에 정규화한 상대 파일 목록을
  비교하며, 서로 다른 DataRaw를 보고 있으면 접속을 중단하고 원인을
  표시합니다. 대용량 파일 내용을 매번 해시하지 않아 시작 지연을
  최소화했습니다.
- XUN DLL 연결 버튼을 접속기 탭에 통합하고 UAC를 통한 관리자 권한
  연결을 자동화했습니다.
- 훅 실행기와 DLL 경로를 각각 입력하거나 파일 선택 창으로 지정할 수
  있습니다. 새 설치의 기본값은 `p4475.exe`와 같은 폴더의
  `p4475-xun-attach.exe`, `p4475-xun.dll`입니다.
- DLL 파일 로그를 접속기에서 켜고 끌 수 있습니다. 비활성화해도 훅과
  XUN 기능은 그대로 작동합니다.
- 한글 닉네임이 XUN 세션 INI를 거칠 때 깨지지 않도록 Windows Unicode
  INI 인코딩을 적용했습니다.

### 외부 자산 가져오기 보강

- 기존 일반 카트 51종, 캐릭터 73종, 펫 24종, 플라잉펫 47종에 더해
  검증된 XUN 카트 후보 100종을 표시합니다. 설치된 항목만 숨기는 필터와
  카테고리별 탭, 검색, 다중 선택을 그대로 지원합니다.
- 최신 클라이언트에만 있는 XUN 세대 표기는 4475이 이해하는 V1 표시
  분류로만 변환하며 실제 BodyParam/KartSpec 물리는 보존합니다.
- XUN 계기판 BML, 일반 Exceed 파형, 부능효과 게이지 및 aura 리소스의
  의존성을 함께 가져옵니다. 이미 설치된 파일과 내용이 충돌하면 임의로
  덮어쓰지 않고 작업을 중단합니다.
- 소스 상점 카탈로그에 개별 ID 행이 없더라도 감사된 모델과 스펙이
  존재하는 XUN 카트는 안전하게 복구해 설치할 수 있습니다.
- 서버 자동 지급 목록은 고정된 두 테스트 차량이 아니라 실제 DataRaw에
  설치되고 검증된 XUN 카트를 동적으로 반영합니다.

### 알려진 제한

- XUN 지원은 정확히 확인된 32비트 언팩 P4475 실행 파일에만 설치되는
  시험 기능입니다. 실행 파일 식별값이나 훅 바이트가 다르면 DLL은
  패치하지 않고 비활성 상태로 남습니다.
- 현재 완성 범위는 기본형 `defaultExceedType` 1~4입니다. 별도의 주행
  스킬 상태 머신이 필요한 타입 5~10은 가져오기 목록에서 숨깁니다.
- XUN 멀티플레이의 프로필 전달 기반은 구현됐지만 여러 클라이언트에서의
  장시간 실전 검증은 아직 진행 중입니다.
- 외부 클라이언트의 원본 게임 데이터는 배포하지 않습니다. 사용자가
  합법적으로 보유한 클라이언트에서 직접 가져와야 합니다.

### 배포 파일

- `p4475-windows-x64.exe`: 서버와 접속기가 통합된 GUI 실행 파일
- `p4475-windows-x64.zip`: 통합 실행 파일, XUN 훅 실행기/DLL,
  `startup-proxy/dinput8.dll` 선택적 시작 프록시, 설정 예시와 문서
- `SHA256SUMS`: 배포 파일 무결성 확인용 SHA-256 목록

전체 변경 비교: `v0.3.0...v1.0.0`

## 0.3.0 — 2026-08-12

- 외부 클라이언트의 트랙, 카트, 캐릭터, 펫, 플라잉펫을 분석하고
  호환 가능한 항목을 DataRaw에 가산 설치하는 워크플로를 추가했습니다.
- 트랙/자산 가져오기 GUI, 진행률 표시, 설치 여부 필터와 비파괴 충돌
  검사를 추가했습니다.
