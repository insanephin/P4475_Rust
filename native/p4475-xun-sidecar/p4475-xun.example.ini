[xun]
; Installs exact-build hooks. Kart-specific tuning is supplied by the server
; through p4475-xun-session.ini and TCP base-port+3; there is no global mock.
enabled=0
; Set to 0 to keep all hooks active without creating or appending the log file.
logging=1
